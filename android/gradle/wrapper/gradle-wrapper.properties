Role: You are a Senior Flutter Developer.Task: Create a fully functional Event Management System for a college using Flutter and Firebase.Constraints:Stack: Flutter (Frontend) + Firebase Firestore (Database) + Firebase Auth (Authentication).Architecture: Use MVVM (Model-View-ViewModel) for clean code separation.State Management: Use Provider or Riverpod.Core Features Required:Unified Authentication:Create a single LoginScreen.When a user logs in, check their role in the Firestore users collection.Registration: Include a toggle switch during Sign Up: "Register as Organizer?". If true, set role: 'organizer', else role: 'student'.Role-Based Navigation (The Logic):If role == 'organizer' $\rightarrow$ Navigate to OrganizerDashboard.If role == 'student' $\rightarrow$ Navigate to StudentFeed.Organizer Dashboard:Floating Action Button: To "Add Event".Create Event Form: Fields for Title, Date, Time, Venue, and Description.My Events List: Show only events created by this organizer with Delete/Edit options.Student Feed:StreamBuilder: Real-time list of all events from the Firestore events collection.Card View: distinct cards showing Event Title, Date, and "Posted By [Organizer Name]".Pull-to-Refresh: standard implementation.Data Structure (Firestore Schema):users: { uid, email, role, displayName }events: { id, title, description, date, location, organizerId, timestamp }Output Requirements:Provide the full code for main.dart (including route handling).Provide the AuthService class (handling Firebase login/registration).Provide the DatabaseService class (handling event CRUD operations).Provide the UI code for the Login, Organizer, and Student screens.Important: Include a brief comment on how to set up the google-services.json file to make this run.